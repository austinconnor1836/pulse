# Recorded Google Places JSON fixtures live here.
# Set PLACES_FIXTURE_DIR=$(pwd)/supabase/functions/_shared/__fixtures__/places to replay.
# Set PLACES_RECORD_DIR=<same path> with a real key to capture on first run.
